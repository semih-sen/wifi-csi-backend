This directory will contain the trained ONNX model file(s).

Place your exported .onnx model here (e.g., csi_activity_model.onnx).
The model path is configured in appsettings.json under "Inference:ModelPath".
